name=file
revision=1
from_source=file
imagedeps="build-essential"

configure() {
	cp -rp ${source_dir}/* .
	./configure --prefix=${prefix}
}

build() {
	make -j ${parallelism}
}

package() {
	make install DESTDIR=${dest_dir}
}
