name=pfetch
revision=1
from_source=pfetch

configure() {
	
	true

}

build() {
	
	true

}

install() {

	mkdir -p ${dest_dir}/${prefix}/bin
	cp ${source_dir}/pfetch ${dest_dir}/${prefix}/bin/pfetch

}
