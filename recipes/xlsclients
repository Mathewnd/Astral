name=xlsclients
revision=2
from_source=xlsclients
imagedeps="gcc"
hostdeps="xgcc xbinutils pkgconfig"
deps="base libxcb xcb-proto"

configure() {
	${source_dir}/configure --prefix=${prefix} --host=x86_64-astral
}

build() {
	make -j${parallelism}
}

package() {
	make DESTDIR=${dest_dir} install
}
