name=xcmsdb
revision=1
from_source=xcmsdb
imagedeps="gcc"
hostdeps="xgcc xbinutils pkgconfig"
deps="base libx11 xorgproto"

configure() {
	${source_dir}/configure --prefix=${prefix} --host=x86_64-astral
}

build() {
	make -j${parallelism}
}

package() {
	make DESTDIR=${dest_dir} install
}
