name=curl
revision=1
from_source=curl
imagedeps="build-essential"
hostdeps="xgcc xbinutils pkgconfig"
deps="base openssl zlib"

configure() {
	cp -rpv ${source_dir}/* .
	CFLAGS="${CFLAGS} -Wno-error=implicit-function-declaration" ./configure --host=x86_64-astral --prefix=${prefix} --with-openssl --with-zlib --disable-ipv6 --with-ca-path=/etc/ssl/certs --disable-static
}

build() {
	CFLAGS="${CFLAGS} -Wno-error=implicit-function-declaration" make -j ${parallelism}
}

package() {
	make install DESTDIR=${dest_dir}
}
