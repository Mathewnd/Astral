name=viewres
revision=1
from_source=viewres
imagedeps="gcc"
hostdeps="xgcc xbinutils pkgconfig"
deps="base libx11 libxt libxaw libxmu xorgproto"

configure() {
	${source_dir}/configure --prefix=${prefix} --host=x86_64-astral
}

build() {
	make -j${parallelism}
}

package() {
	make DESTDIR=${dest_dir} install
}
