name=xwininfo
revision=2
from_source=xwininfo
imagedeps="gcc"
hostdeps="xgcc xbinutils pkgconfig"
deps="base libx11 libxcb libiconv xcb-proto xorgproto"

configure() {
	${source_dir}/configure --prefix=${prefix} --host=x86_64-astral
}

build() {
	make -j${parallelism}
}

package() {
	make DESTDIR=${dest_dir} install
}
