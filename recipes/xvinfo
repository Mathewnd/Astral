name=xvinfo
revision=2
from_source=xvinfo
imagedeps="gcc"
hostdeps="xgcc xbinutils pkgconfig"
deps="base libxv libx11 libxext xorgproto"

configure() {
	${source_dir}/configure --prefix=${prefix} --host=x86_64-astral
}

build() {
	make -j${parallelism}
}

package() {
	make DESTDIR=${dest_dir} install
}
