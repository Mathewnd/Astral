name=libgomp
revision=2
from_source=gcc
imagedeps="build-essential"
hostrundeps="xgcc xbinutils libstdc++-v3"
deps="gcc"

build() {
	cd ${build_dir}/builds/gcc
	make all-target-libgomp -j${parallelism}
}

package() {
	cd ${build_dir}/builds/gcc
	make DESTDIR="${dest_dir}" install-target-libgomp
}
