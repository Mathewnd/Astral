name=fonts-dejavu
from_source=fonts-dejavu
revision=2

build() {
	true
}

package() {
	install -v -d -m755 ${dest_dir}/${prefix}/share/fonts/dejavu
	install -v -m644 ${source_dir}/ttf/*.ttf ${dest_dir}/${prefix}/share/fonts/dejavu
}
