diff --git sdl2-clean/CMakeLists.txt sdl2-workdir/CMakeLists.txt
index 8f9ab39..3361ebd 100644
--- sdl2-clean/CMakeLists.txt
+++ sdl2-workdir/CMakeLists.txt
@@ -1690,7 +1690,7 @@ elseif(UNIX AND NOT APPLE AND NOT RISCOS AND NOT HAIKU)
     if(FREEBSD OR NETBSD OR OPENBSD OR BSDI)
       CheckUSBHID()
     endif()
-    if(LINUX AND HAVE_LINUX_INPUT_H AND NOT ANDROID)
+    if((LINUX OR CMAKE_SYSTEM_NAME STREQUAL "Astral") AND HAVE_LINUX_INPUT_H AND NOT ANDROID)
       set(SDL_JOYSTICK_LINUX 1)
       file(GLOB JOYSTICK_SOURCES ${SDL2_SOURCE_DIR}/src/joystick/linux/*.c ${SDL2_SOURCE_DIR}/src/joystick/steam/*.c)
       list(APPEND SOURCE_FILES ${JOYSTICK_SOURCES})
